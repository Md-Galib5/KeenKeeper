import React from 'react';
import { Outlet } from 'react-router';


const Layout = () => {
    return (
        <div>
            <Navbar />
            
        </div>
    );
};

export default Layout;